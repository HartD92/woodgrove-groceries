### 2026-07-22: Downstream API scope provisioning for storefront calls
**By:** Trinity
**What:** Use `access_as_user` as the delegated scope for the graph-middleware downstream API, with identifier URI `api://woodgrove-graph-middleware-{environment}`. The storefront now deploys deterministic downstream API resource URIs/scopes for `WoodgroveGroceriesApi` and `GraphApiMiddleware`, and Entra provisioning grants web→API plus web→graph-middleware consent for `access_as_user`.
**Why:** The live profile failure was a two-part downstream API registration/configuration mismatch: the API app was provisioned and consented but the web app still deployed placeholder resource/scope values, while graph-middleware had no delegated exposed scope, no web requiredResourceAccess entry, and no web→graph-middleware OAuth2 permission grant. Graph-middleware does not validate a special `User.MFA` scope in code; it only requires an authenticated JWT bearer token, so `access_as_user` keeps delegated scope naming consistent with the existing API app.
