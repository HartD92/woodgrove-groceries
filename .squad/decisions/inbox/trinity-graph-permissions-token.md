### 2026-07-22: Complete app-only Graph permissions and /token diagnosis
**By:** Trinity
**What:** Add Microsoft Graph application roles `AuditLog.Read.All`, `AppRoleAssignment.ReadWrite.All`, `UserAuthenticationMethod.Read.All`, and `UserAuthenticationMethod.ReadWrite.All` to the CIAM provisioning for the storefront service principal; add the audit/authentication-method roles to graph-middleware too. Existing `User.ReadWrite.All`, `Group.ReadWrite.All`, and `Directory.ReadWrite.All` remain.
**Why:** Live profile app-only calls now authenticate but lack authorization for `users/{id}?$select=signInActivity` and `users/{id}/authentication/*`. Storefront makes the failing profile reads/writes via `MicrosoftGraph:ClientId=web app`; graph-middleware also makes app-only user profile writes and shares the helper pattern. Live /Token requests successfully acquire the API token and call `/account` with HTTP 200; the remaining diagnostic-page error is the Account API's separate downstream Payment OBO demo, which has no provisioned Payment API resource in this monorepo/deployment.
