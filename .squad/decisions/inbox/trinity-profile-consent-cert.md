### 2026-07-22: Profile consent and app-only Graph credential fixes
**By:** Trinity
**What:** Add Microsoft Graph delegated `User.Read` to the storefront app registration and admin grant, keep web→API and web→graph-middleware grants, and pre-authorize the storefront client on both custom API resources for `access_as_user`. Move app-only Graph helper code away from CurrentUser/My certificate dependency by preferring `MicrosoftGraph:ClientSecret` credentials. Deploy app-only MicrosoftGraph settings for storefront and graph-middleware; create a Key Vault-backed `graph-client-secret` for graph-middleware.
**Why:** Live ExtID inspection showed web→API and web→graph-middleware AllPrincipals grants exist, resource identifier URIs exactly match the requested custom API scopes, and the provisioning FIC already has `Application.ReadWrite.All`, `DelegatedPermissionGrant.ReadWrite.All`, and `AppRoleAssignment.ReadWrite.All`. The remaining profile-read AADSTS65001 was the storefront's Microsoft Graph `User.Read` request, which was configured in `GraphApi:Scopes` but not required/granted in Entra. Custom API `preAuthorizedApplications` were empty, so provisioning now also sets them as a CIAM-safe belt-and-suspenders measure. The separate certificate failures came from legacy app-only Graph helper code that required an undeployed certificate in `CurrentUser/My`; client secrets are already managed through Key Vault for this cloud demo.
